package com.contract.master.shared.domain.base;

public interface AggregateRoot {
    // Aggregate roots are typically entities with global identity.
    // This interface serves as a marker for DDD aggregate roots.
}
